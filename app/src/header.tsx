import React from "react";
import Button from "./button";

const Header:React.FC<any> = () => (
      <div>
        <Button title="about"/>
        <Button title="about"/>
      </div>
);

export default Header;